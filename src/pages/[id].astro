---
const { id } = Astro.params;

const page = await getPage(id);

if (!page) {
  const error = await fetch(`${Astro.url.origin}/404`)
  return new Response(error.body, {
    headers: error.headers,
    status: 404,
    statusText: 'Not Found'
  })
}
---

{page}